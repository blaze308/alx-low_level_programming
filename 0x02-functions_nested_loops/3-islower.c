#include "main.h"
/**
* _islower - function to print lower case
* @c: printed parameter
* Return: 1 if lower case else o
*/

int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
